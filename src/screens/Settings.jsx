import {
    View,
} from 'react-native';
import {
    useEffect
} from 'react';
import Styles from '../style';

const Settings = (props) => {
    useEffect(() => {
    }, [])
   
    return (
        <View style={Styles}>
            
        </View>
    )
}

export default Settings;